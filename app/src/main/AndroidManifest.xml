<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for Supabase API -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <queries>
        <package android:name="com.google.android.gms" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaskFlow">

        <activity
            android:name=".ui.attachment.FullscreenImageActivity"
            android:exported="false"
            android:theme="@style/Theme.TaskFlow" />

        <!-- Auth - Đăng nhập (Launcher) -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.system.ShortcutRouterActivity"
            android:exported="true" />

        <activity
            android:name=".ui.system.ShareToTaskActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <!-- Dashboard -->
        <activity
            android:name=".ui.dashboard.DashboardActivity"
            android:windowSoftInputMode="adjustPan"
            android:exported="false" />

        <!-- Auth -->
        <activity
            android:name=".ui.auth.ForgotPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".ui.auth.ResetPasswordActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="reset-password"
                    android:scheme="taskai" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.auth.SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".ui.auth.GithubAuthCallbackActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="auth-callback"
                    android:scheme="taskai" />
            </intent-filter>
        </activity>

        <!-- Project -->
        <activity android:name=".ui.project.CreateProjectActivity" />
        <activity
            android:name=".ui.ai.AiCreateActivity"
            android:theme="@style/Theme.TaskFlow.Translucent"
            android:windowSoftInputMode="adjustResize|stateVisible"
            android:exported="false" />
        <activity
            android:name=".ui.project.ProjectBoardActivity"
            android:exported="false" />
        <activity
            android:name=".ui.foryou.ForYouActivity"
            android:exported="false" />
        <activity
            android:name=".ui.timeline.ProjectDetailActivity"
            android:exported="false" />
        <activity
            android:name=".ui.project.CalendarActivity"
            android:exported="false" />
        <activity
            android:name=".ui.project.TrashActivity"
            android:exported="false" />
        <activity
            android:name=".ui.project.ProjectActivityHistoryActivity"
            android:exported="false" />
        <activity
            android:name=".ui.project.TaskDetailActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ui.project.FocusModeActivity"
            android:exported="false"
            android:theme="@style/Theme.TaskFlow" />
        <!-- Notification -->
        <activity android:name=".ui.notification.NotificationsActivity" />

        <!-- Profile -->
        <activity
            android:name=".ui.profile.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".ui.profile.ProjectTrashActivity"
            android:exported="false" />

        <!-- Member -->
        <activity android:name=".ui.member.MemberListActivity" />
        <activity android:name=".ui.member.InviteMemberActivity" />

        <!-- MainActivity cũ -->
        <activity android:name=".MainActivity" />

        <service
            android:name=".ui.system.StickyTaskService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <receiver
            android:name=".ui.system.StickyTaskActionReceiver"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name=".ui.widget.TaskTodayWidgetProvider"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/task_today_widget_info" />
        </receiver>

        <receiver
            android:name=".ui.widget.TaskTodaySummaryWidgetProvider"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/task_today_summary_widget_info" />
        </receiver>

        <!-- ✅ FileProvider để share URI ảnh camera -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>

</manifest>
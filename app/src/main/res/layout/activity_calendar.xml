<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_light">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- TOP APP BAR (Brought over from Timeline) -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:paddingHorizontal="16dp">

            <ImageView
                android:id="@+id/imgUserAvatar"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:src="@drawable/bg_avatar_grey_bordered"
                android:scaleType="centerCrop"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent" />

            <ImageButton
                android:id="@+id/btnBack"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_marginStart="4dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/back"
                android:src="@drawable/ic_chevron_left"
                app:tint="@color/theme_text_primary"
                app:layout_constraintStart_toEndOf="@id/imgUserAvatar"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent" />

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:gravity="center"
                app:layout_constraintStart_toEndOf="@id/btnBack"
                app:layout_constraintEnd_toStartOf="@id/layoutRightIcons"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent">

                <TextView
                    android:id="@+id/tvProjectName"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                        android:text="@string/project_name_label"
                    android:textColor="@color/theme_text_primary"
                    android:textSize="16sp"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/tvMonth"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text=""
                    android:textColor="@color/theme_text_hint"
                    android:textSize="12sp" />
            </LinearLayout>

            <LinearLayout
                android:id="@+id/layoutRightIcons"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent">

                <ImageButton
                    android:id="@+id/btnMoreOptions"
                    android:layout_width="36dp"
                    android:layout_height="36dp"
                    android:layout_marginStart="8dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="@string/options"
                    android:src="@drawable/ic_more_vert"
                    app:tint="@color/theme_text_secondary" />
            </LinearLayout>
        </androidx.constraintlayout.widget.ConstraintLayout>

        <!-- VIEW TOGGLE (Timeline/Board/Calendar) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingHorizontal="16dp">

            <!-- Timeline -->
            <TextView
                android:id="@+id/tabTimeline"
                android:layout_width="wrap_content"
                android:layout_height="34dp"
                android:background="@drawable/bg_tab_inactive"
                android:gravity="center"
                android:paddingHorizontal="18dp"
                android:text="Timeline"
                android:textColor="@color/theme_text_secondary"
                android:textSize="13sp" />

            <!-- Board -->
            <TextView
                android:id="@+id/tabBoard"
                android:layout_width="wrap_content"
                android:layout_height="34dp"
                android:layout_marginStart="8dp"
                android:background="@drawable/bg_tab_inactive"
                android:gravity="center"
                android:paddingHorizontal="18dp"
                android:text="Board"
                android:textColor="@color/theme_text_secondary"
                android:textSize="13sp" />

            <!-- Calendar (active in this view) -->
            <TextView
                android:id="@+id/tabCalendar"
                android:layout_width="wrap_content"
                android:layout_height="34dp"
                android:layout_marginStart="8dp"
                android:background="@drawable/bg_tab_active"
                android:gravity="center"
                android:paddingHorizontal="18dp"
                android:text="Calendar"
                android:textColor="@color/white"
                android:textSize="13sp"
                android:textStyle="bold" />

            <!-- Spacer để đẩy Nút Today sát lề phải -->
            <View android:layout_width="0dp" android:layout_height="0dp" android:layout_weight="1"/>

            <!-- Nút Today (góc phải dưới top bar) -->
            <TextView
                android:id="@+id/btnToday"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/calendar_today"
                android:textSize="14sp"
                android:textStyle="bold"
                android:textColor="@color/primary"
                android:padding="8dp"
                android:background="?attr/selectableItemBackgroundBorderless" />
        </LinearLayout>

        <!-- Month Navigator (Nơi hiển thị tháng hiện tại) -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingHorizontal="24dp"
            android:paddingVertical="8dp">

            <ImageView
                android:id="@+id/btnPrevMonth"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_alignParentStart="true"
                android:src="@drawable/ic_chevron_left"
                app:tint="@color/theme_text_secondary" />

            <TextView
                android:id="@+id/tvMonthYear"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                        android:text="@string/calendar_month_placeholder"
                android:textColor="@color/theme_text_primary"
                android:textSize="16sp"
                android:textStyle="bold" />

            <ImageView
                android:id="@+id/btnNextMonth"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_alignParentEnd="true"
                android:src="@drawable/ic_chevron_right"
                app:tint="@color/theme_text_secondary" />
        </RelativeLayout>
        <androidx.gridlayout.widget.GridLayout
            android:id="@+id/glCalendar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:columnCount="7"
            android:paddingHorizontal="16dp"
            android:paddingBottom="8dp">
            <TextView android:text="@string/calendar_weekday_sun" android:gravity="center" android:textColor="@color/theme_text_secondary" android:textSize="12sp" android:layout_width="0dp" app:layout_columnWeight="1"/>
            <TextView android:text="@string/calendar_weekday_mon" android:gravity="center" android:textColor="@color/theme_text_secondary" android:textSize="12sp" android:layout_width="0dp" app:layout_columnWeight="1"/>
            <TextView android:text="@string/calendar_weekday_tue" android:gravity="center" android:textColor="@color/theme_text_secondary" android:textSize="12sp" android:layout_width="0dp" app:layout_columnWeight="1"/>
            <TextView android:text="@string/calendar_weekday_wed" android:gravity="center" android:textColor="@color/theme_text_secondary" android:textSize="12sp" android:layout_width="0dp" app:layout_columnWeight="1"/>
            <TextView android:text="@string/calendar_weekday_thu" android:gravity="center" android:textColor="@color/theme_text_secondary" android:textSize="12sp" android:layout_width="0dp" app:layout_columnWeight="1"/>
            <TextView android:text="@string/calendar_weekday_fri" android:gravity="center" android:textColor="@color/theme_text_secondary" android:textSize="12sp" android:layout_width="0dp" app:layout_columnWeight="1"/>
            <TextView android:text="@string/calendar_weekday_sat" android:gravity="center" android:textColor="@color/theme_text_secondary" android:textSize="12sp" android:layout_width="0dp" app:layout_columnWeight="1" android:layout_marginBottom="8dp"/>

        </androidx.gridlayout.widget.GridLayout>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvTasks"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_bottom_sheet"
            android:elevation="8dp"
            android:paddingHorizontal="16dp"
            android:paddingTop="12dp"
            android:paddingBottom="@dimen/bottom_nav_padding"
            android:clipToPadding="false" />
    </LinearLayout>

    <!-- Include shared bottom bar layout (Direct child with gravity to match Dashboard) -->
    <include
        android:id="@+id/includeBottomBar"
        layout="@layout/layout_bottom_nav_fab"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btnNewTask"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_anchor="@id/includeBottomBar"
        app:layout_anchorGravity="top|center"
        android:src="@drawable/ic_add"
        app:backgroundTint="@color/primary"
        app:tint="@color/white"
        app:elevation="16dp"
        android:layout_marginBottom="16dp"
        app:borderWidth="0dp" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>